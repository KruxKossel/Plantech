@model IEnumerable<Plantech.ViewModels.CulturasPerdidasViewModel>

@{
    ViewData["Title"] = "Culturas Perdidas";
}

<br><br>

@if (User.IsInRole("Administrador") || User.IsInRole("Agricultor"))
{
    <div class="card-index">
    <div style="max-height: 350px; overflow-y: auto;">
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>
                        Cultura Perdida Nome
                    </th>
                    <th>
                        Data da Colheita
                    </th>
                    <th>
                        Hortaliça
                    </th>
                    <th>
                        Quantidade
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.CulturaPerdida.Nome)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.CulturaPerdida.Colheita.DataColheita)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.CulturaPerdida.Colheita.Plantio.Hortalica.Nome)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Quantidade)
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
<br>
<div>
    <a asp-controller="Agricultor" asp-action="Index" class="btn btn-primary">Voltar a Home</a>
</div>

}